import React from 'react';

const LoadingSpinner = () => {
    return (
        <div className='flex justify-center mt-[190px]'>
            <span className="loading loading-spinner text-error w-[160px]"></span>
        </div>
    );
};

export default LoadingSpinner;